with open("file.txt") as w:
    w.seek(2)
    result = []
    lines = w.readlines()
    for product in len(lines) / 5:
        result.append({
            "name": product,
            "price"
        })

    current_object = line
    current_price = current_price
        pass
    if len(result) == 0:
        print("An empty file was given. There is nothing to sort.")
